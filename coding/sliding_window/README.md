## Sliding Window Patterns

### Longest Substring Without Repeating Characters (LC 3)
- Trigger: longest subarray/substring with unique elements
- Pattern: sliding window + set
- Time / Space: O(n) / O(n)
- Pitfall: shrink window only when duplicate appears
